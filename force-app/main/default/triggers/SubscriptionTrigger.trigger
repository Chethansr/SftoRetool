trigger SubscriptionTrigger on SBQQ__Subscription__c (before insert, after insert, before update, after update, before delete, after delete) {
    //SubscriptionTriggerHandler.subscriptionTriggerHandler();
}